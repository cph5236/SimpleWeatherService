import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.cph5236.simpleweatherservice',
  appName: 'Simple Weather Service',
  webDir: 'dist',
}

export default config
